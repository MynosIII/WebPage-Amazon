name: Site quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: pip install Pillow==11.3.0
      - name: Verify generated pages and discovery files
        run: |
          python scripts/build_homepages.py --check
          python scripts/build_case_studies.py --check
          python scripts/build_about_pages.py --check
          python scripts/build_cv_pages.py --check
          python scripts/sync_case_tools.py --check
          python scripts/sync_ecommerce_claims.py --check
          python scripts/apply_legacy_translation_fixes.py --check
          python scripts/validate_localization.py
          python scripts/validate_project_claims.py
          python scripts/generate_sitemap.py --check
          python scripts/add_image_dimensions.py --check
          python scripts/build_catalog_static.py --check
      - name: Validate links, anchors, metadata and asset casing
        run: python scripts/validate_site.py

  browser-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - name: Validate generated homepage HTML
        run: pnpm run validate:html
      - name: Responsive and accessibility regression tests
        run: pnpm test
      - name: Lighthouse budgets
        run: pnpm run lighthouse
      - name: Upload responsive screenshots and reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: browser-quality-artifacts
          path: |
            test-results/
            playwright-report/
            .lighthouseci/
          if-no-files-found: ignore
